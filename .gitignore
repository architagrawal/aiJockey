.venv/
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# audio + data (user-supplied, often large)
clips/
cache/
output/
tests/fixtures/

# samples: ignore audio files, keep README + manifest stub
samples/**/*.wav
samples/**/*.mp3
samples/**/*.flac
samples/**/*.ogg
samples/**/*.m4a

# references: ignore reference JSON (may contain copyrighted analysis), keep README
references/*.json
!references/README.md

# Claude Code personal settings
.claude/

# Local-only scripts (not for public push — copyright caution)
scripts/bulk_download_clips.py
clips_test/
clips_demo/

# training artifacts (raw audio + features) — but keep tracklist JSONs (just URLs+timestamps)
datasets/raw_mixes/
datasets/transitions_real/
datasets/synthetic_transitions.npz
checkpoints/

# editor
.vscode/
.idea/
*.swp
.DS_Store
